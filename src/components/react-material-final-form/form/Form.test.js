// component contract

/*

    1. What it renders (which may be nothing)

    2. The props the component receives

    3. The state the component holds (if any)

    4. What the component does when the user interacts with it (via clicking, dragging, keyboard input, etc)

    more...

    - The context the component is rendered in
    - What the component does when you call methods on its instance (public ref interface)
    - Side effects that occur as part of the component lifecycle (componentDidMount, componentWillUnmount, etc)    


*/